# Skilldo config using Claude CLI (no API key needed — uses claude's own auth)
# Usage: skilldo generate . --config skilldo-claude.toml -o SKILL.md

[llm]
provider_type = "cli"
model = "claude-sonnet-4-6"
cli_command = "claude"
cli_args = ["-p", "--no-session-persistence", "--model", "claude-sonnet-4-6", "--output-format", "text"]
request_timeout_secs = 900

[generation]
language = "rust"
parallel_extraction = false  # CLI provider is sequential
max_retries = 3
enable_test = true
enable_review = true
test_mode = "thorough"
security_threshold = 85

custom_instructions = """
ALWAYS use crate-root imports: `use llmposter::{ServerBuilder, Fixture, Provider, ToolCall, FailureConfig, StreamingConfig, MockServer}`.
NEVER use submodule paths EXCEPT for `FixtureResponse` which is only available via `use llmposter::fixture::FixtureResponse`.
The server binds to port 0 (OS-assigned) — use `server.url()` to get the base URL.
Code examples use `#[tokio::main]` with `async fn main()`.
The builder method is `.build().await` — there is NO `.run()` method. Never use `.run()`.
`with_bearer_token(token)` implicitly enables auth checking — no separate `.with_auth(true)` needed.
When `with_bearer_token()` is configured, requests without a valid `Authorization: Bearer <token>` header receive HTTP 401. Include a code example that demonstrates this: create a server with auth, send a request without the Bearer header using reqwest, and assert the response status is 401.
`expires_after_uses()` does not exist — do NOT reference it. Use `with_bearer_token_uses(token, max)` instead.
Only import types that are actually used in code examples — no unused imports.
Every method used in a code example MUST have a corresponding entry in the ## API Reference section.
Fenced code blocks showing SSE events or plain text output must use ```text language tag, not bare ```.
"""

[generation.container]
install_source = "registry"
timeout = 120

# Review also uses Claude CLI
[generation.review_llm]
provider_type = "cli"
model = "claude-sonnet-4-6"
cli_command = "claude"
cli_args = ["-p", "--no-session-persistence", "--model", "claude-sonnet-4-6", "--output-format", "text"]
request_timeout_secs = 900
