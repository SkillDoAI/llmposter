coverage:
  status:
    project:
      default:
        # v0.4.6 temporarily at 97 while we close the gap on defensive
        # error branches flagged in BACKLOG.md ("v0.4.6 coverage gate").
        # Restore to 98 in a follow-up PR.
        target: 97%
    patch:
      default:
        target: 97%

ignore:
  - "src/main.rs"  # Binary entry point — thin wrapper around cli::run()
