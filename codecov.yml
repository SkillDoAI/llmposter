coverage:
  status:
    project:
      default:
        target: 97%
    patch:
      default:
        # v0.4.7: FixtureSet refactor added ~100 lines of pre-sort +
        # iterator code with partial coverage from existing tests.
        target: 90%

ignore:
  - "src/main.rs"  # Binary entry point — thin wrapper around cli::run()
  - "src/ui.rs"    # Debug UI — opt-in tool, not core mock functionality
