coverage:
  status:
    project:
      default:
        target: 98%
    patch:
      default:
        target: 98%

ignore:
  - "src/main.rs"  # Binary entry point — thin wrapper around cli::run()
  - "src/ui.rs"    # Debug UI — opt-in tool, not core mock functionality
